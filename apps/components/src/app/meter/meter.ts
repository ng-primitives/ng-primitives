import { Component } from '@angular/core';
import { NgpMeter } from 'ng-primitives/meter';

@Component({
  selector: 'app-meter',
  hostDirectives: [NgpMeter],
  template: ``,
  styles: ``,
})
export class Meter {}
