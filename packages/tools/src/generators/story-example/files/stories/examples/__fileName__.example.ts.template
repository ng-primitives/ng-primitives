import { Component } from '@angular/core';
import { <%= directive %> } from '<%= package %>';

@Component({
  standalone: true,
  selector: '<%= selector %>',
  imports: [<%= directive %>],
  template: ``,
})
export class <%= component %> {}

