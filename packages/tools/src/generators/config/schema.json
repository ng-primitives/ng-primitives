{
  "$schema": "http://json-schema.org/schema",
  "$id": "Config",
  "title": "",
  "type": "object",
  "properties": {
    "primitive": {
      "type": "string",
      "description": "The primitive to add the config to.",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "x-prompt": "What primitive should the config be added to?"
    }
  },
  "required": ["primitive"]
}
