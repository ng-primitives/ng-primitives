/**
 * Placeholder for signal-forms (`@angular/forms/signals`) integration tests.
 *
 * The installed Angular version (20.3.9) does not yet ship the signal-forms
 * subpath. Once available, mirror the scenarios in `forms.spec.ts`:
 *  - initial value reflected in DOM
 *  - click updates the underlying signal
 *  - setting the signal externally updates the DOM
 *  - regression: writeValue must not loop back through onChange
 */
describe.skip('Toggle (reusable component) — signal forms', () => {
  it.todo('binds with form(model) signal forms API');
});
