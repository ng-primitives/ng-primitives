/**
 * Placeholder fixture for signal-forms (`@angular/forms/signals`) integration tests.
 *
 * The installed Angular version (20.3.9) does not yet ship the signal-forms
 * subpath. Once available, define a `Toggle` component here that mirrors
 * `apps/components/.../reusable-components/toggle/toggle.ts` but wires up
 * the signal-forms `[control]` API instead of `ControlValueAccessor`.
 */
export {};
