import { NgpAvatarDirective } from './avatar.directive';

describe('NgpAvatarDirective', () => {
  it('should create an instance', () => {
    const directive = new NgpAvatarDirective();
    expect(directive).toBeTruthy();
  });
});
